var config = require('./config');
var Element = require('../ui').Element;
var TextArea = require('../ui').TextArea;
var Animation = require('../animate').Animation;
var SpriteSheet = require('../animate').SpriteSheet;

var getElements = exports.getElements = function(level) {
	var elements = [];
	
	switch (level) {
		/*
		case 1:
			var textOpts = {
				pos: "bottom-center",
				size: [200,35],
				margin: 5,
				text: "We're in big trouble!",
				scrolling: true
			};
			
			//var test = new Element(testElementOpts);
			var text = new TextArea(textOpts);
			console.log('i');
			//elements.push(test);
			elements.push(text);
			break;
			*/
	}
	return elements;
};